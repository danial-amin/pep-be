# Use official Langfuse Docker image
FROM langfuse/langfuse:latest

# Expose the default Langfuse port
EXPOSE 3000

# The entrypoint is already set in the base image
# Environment variables will be passed at runtime
